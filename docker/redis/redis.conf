# 보안: 비밀번호 설정 (필요 없으면 주석 처리)
# requirepass password

# 메모리 관리: 최대 256MB 사용
maxmemory 256mb

# 메모리 꽉 찼을 때: 가장 오랫동안 안 쓴 키부터 삭제 (LRU 알고리즘)
maxmemory-policy allkeys-lru

# 데이터 영속성: 900초 동안 1개 이상의 키가 변하면 스냅샷 저장
save 900 1

# 연결 허용: 모든 IP에서 접속 가능 (Docker 환경용)
bind 0.0.0.0